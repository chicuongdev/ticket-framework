package io.hrc.testing.inventory;

/**
 * Thread-safe (AtomicLong). Zero-oversell guaranteed.
 * Inspection: getCurrentAvailable(), getReserveCallCount(),
 *             getOversellAttemptCount(), reset().
 */
public class InMemoryInventoryStrategy {}
