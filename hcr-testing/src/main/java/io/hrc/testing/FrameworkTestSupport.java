package io.hrc.testing;

/**
 * Utility factory for test setup.
 * Factory: inMemoryInventory(), mockPayment(), inMemoryEventBus().
 * Concurrency: simulateConcurrentRequests().
 * Assertions: assertNoOversell(), assertThroughputAbove(), assertEventPublished().
 */
public class FrameworkTestSupport {}
